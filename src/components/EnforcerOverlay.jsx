import React from "react";

export const EnforcerOverlay = ({ level, config }) => {
  // Desktop Eclipse is handled via Electron windows.
  // Sonic Escalation is handled via Web Audio in App.jsx.
  return null;
};
